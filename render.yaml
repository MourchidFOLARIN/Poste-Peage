services:
  # ─────────────────────────────────────────
  # 1. SERVICE BACKEND API (Docker Node.js)
  # ─────────────────────────────────────────
  - type: web
    name: peage-backend
    env: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./Dockerfile

    # Variables d'environnement injectées ou configurées sur le Dashboard Render
    envVars:
      - key: DATABASE_URL
        sync: false # Utilise l'URL de connexion de la base créée manuellement sur Render
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production

  # ──────────────────────────────────────────
  # 2. SERVICE FRONTEND (React / Vite — Static)
  # ──────────────────────────────────────────
  - type: web
    name: peage-frontend
    env: static
    buildCommand: cd frontend && npm install --include=dev && npm run build
    staticPublishPath: frontend/dist
    # Redirige toutes les routes vers index.html (React Router SPA)
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://peage-backend.onrender.com
